<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Hello Page</title>
</head>
<body>
    <h1>
        Hello there 
        <% if (typeof user !== 'undefined' && user) { %>
            <%= user.name %>
        <% } %>
    </h1>
</body>
</html>
